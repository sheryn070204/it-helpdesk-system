// This file helps turn your special CSS into normal CSS that browsers understand
const config = {
  plugins: {
    // This plugin enables Tailwind CSS styles
    "@tailwindcss/postcss": {},
  },
};

// Export the config so the computer can use it
export default config;
